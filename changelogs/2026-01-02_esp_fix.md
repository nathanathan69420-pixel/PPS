# Update Log - 2026-01-02

### General
- Reverted all script file extensions from `.luau` back to `.lua` as requested.
- Updated `main.lua` loader to link to `.lua` extensions.
- Standardized bypass logic across all scripts.
- Unified footers to `by RwalDev & Plow | 1.7.3 | Discord: .gg/UuyxhqgEVs` across all scripts.

### Blind Shot
- **Fixed ESP**: Replaced the external `Twilight` dependency with a custom, high-performance internal ESP.
    - Added **8-corner 3D Box ESP** for precise tracking.
    - Added **2D Bounding Box ESP**.
    - Added **Chams** with customizable colors and depth mode support.
- **Improved Bypass**: Implemented the Axis Hub metatable bypass to prevent detection by specialized game scripts.
- **Feature Cleanup**: Refactored Auto Aim, Auto Punch, and Movement features for better reliability and performance.
- **UI Rewrite**: Standardized the menu layout to match the Axis Hub design language.

### Violence District
- **Fixed Generator ESP**: 
    - Added a dropdown to select between **Highlight** and **Box** ESP types.
    - Highlights now feature customizable colors and are visible through walls (**AlwaysOnTop**).
    - Box mode provides a clear 2D bounding box without distracting text labels.
- **Fixed Box ESP**: Re-implemented the bounding box calculation using a 8-point projection method for perfect alignment.
- **Killer & Survivor ESP**: Replaced text labels with team-based highlights (Crimson for Killers, Deep Blue for Survivors).
- **Chams**: Added a color picker to the Chams toggle (default: White).
- **UI Improvements**: 
    - Updated window titles and other text elements to reflect the `.lua` extension change.
    - Names are now automatically hidden when highlighting (Chams, Killer ESP, or Survivor ESP) is enabled.
- **Improved Bypass**: Enhanced the metatable bypass to handle `GuiService`, `LogService`, and specialized detection vectors.

### Other Scripts
- Standardized file extensions and updated internal UI titles (e.g., `Flick.lua`, `Operation One.lua`).
- Improved generic bypass functions for `VirtualInputManager` and `HttpService`.
